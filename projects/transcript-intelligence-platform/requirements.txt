# Core
pydantic>=2.0,<3.0
tenacity>=8.2

# ETL (PySpark optional — falls back to simulation in CI)
pyspark>=3.4; extra == "spark"

# Dashboard (Bullet 4)
streamlit>=1.35
plotly>=5.20
pandas>=2.1

# AWS SDK (optional — mock mode available without credentials)
boto3>=1.34; extra == "aws"
botocore>=1.34; extra == "aws"

# Testing
pytest>=8.0
pytest-cov>=5.0

# Dev tools
black>=24.0
ruff>=0.4
