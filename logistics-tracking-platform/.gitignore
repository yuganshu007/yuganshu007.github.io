# Python
.venv/
__pycache__/
*.pyc

# Java / Maven build output
tracking-worker/target/
tracking-worker/dependency-reduced-pom.xml

# SQLite databases (regenerated by services/benchmarks)
data/*.db
data/*.db-shm
data/*.db-wal

# Keep benchmark result JSON snapshots (committed as evidence)
!data/*.json
